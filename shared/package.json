{
  "name": "@cam-labs/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript domain models & types for CAM LABS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
